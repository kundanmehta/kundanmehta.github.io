( function( $ ) {

	"use strict";

  $(".casestudy-outerarea").tilt({
    maxTilt: 16,
    perspective: 2000,
    easing: "cubic-bezier(.03,.98,.52,.99)",
    speed: 1200,
    glare: true,
    maxGlare: 0.3
    
  });
  
 
}( jQuery ) );




